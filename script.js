const toggle=document.querySelector('.menu-toggle');const links=document.querySelector('.nav-links');if(toggle&&links){toggle.addEventListener('click',()=>links.classList.toggle('open'));links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')))}
const lb=document.querySelector('.lightbox');const lbImg=lb?.querySelector('img');const closeBtn=lb?.querySelector('.lightbox-close');const prevBtn=lb?.querySelector('.lightbox-prev');const nextBtn=lb?.querySelector('.lightbox-next');const cap=lb?.querySelector('.lightbox-caption');let items=[];let current=0;
function collect(){items=[...document.querySelectorAll('[data-img]')].map(el=>({src:el.dataset.img,title:el.dataset.title||el.querySelector('span')?.textContent||el.querySelector('img')?.alt||'ULS English'}));}
function show(i){if(!lb||!lbImg)return;current=(i+items.length)%items.length;lbImg.src=items[current].src;cap.textContent=items[current].title;lb.classList.add('open');lb.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}
collect();document.querySelectorAll('[data-img]').forEach((el,i)=>el.addEventListener('click',()=>show(i)));function close(){lb.classList.remove('open');lb.setAttribute('aria-hidden','true');document.body.style.overflow='';}
closeBtn?.addEventListener('click',close);prevBtn?.addEventListener('click',e=>{e.stopPropagation();show(current-1)});nextBtn?.addEventListener('click',e=>{e.stopPropagation();show(current+1)});lb?.addEventListener('click',e=>{if(e.target===lb)close()});document.addEventListener('keydown',e=>{if(!lb?.classList.contains('open'))return;if(e.key==='Escape')close();if(e.key==='ArrowLeft')show(current-1);if(e.key==='ArrowRight')show(current+1)});
let sx=0;lb?.addEventListener('touchstart',e=>sx=e.touches[0].clientX,{passive:true});lb?.addEventListener('touchend',e=>{let dx=e.changedTouches[0].clientX-sx;if(Math.abs(dx)>60)show(current+(dx<0?1:-1))},{passive:true});